# Recall UI

Three-column UI:
- Left: conversation history
- Middle: chat window (topic inferred from the first user message)
- Right: quiz with selected-topics banner + questions

## Configure API

Set `VITE_API_BASE_URL` to your backend base URL (defaults to `http://localhost:8000`).

## Run

```bash
npm install
npm run dev
```
